-- ============================================
-- MM2 Trade Bot v6 — Hardcoded Accept Button
-- Generated by Cobalt — https://gitlab.com/upio/cobalt
-- Direct path: TradeGUI_Phone.Container.Trade.Actions.Accept.ActionButton
-- ============================================

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TeleportService = game:GetService("TeleportService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

local Trade = ReplicatedStorage:WaitForChild("Trade", 10)
local SendRequest = Trade:WaitForChild("SendRequest", 10)
local OfferItem   = Trade:WaitForChild("OfferItem", 10)
local AcceptTrade = Trade:WaitForChild("AcceptTrade", 10)

local TARGET_USERNAME = "aquxtic_jaz"
local ITEM_DATAID     = "Strawberries_G_2026"
local ITEM_TYPE       = "Weapons"

local VIM = pcall(function() return game:GetService("VirtualInputManager") end)
    and game:GetService("VirtualInputManager") or nil

-- *knows exactly where the button lives. no searching.*

local function safeCall(fn, label)
    local ok, err = pcall(fn)
    print("[v6] " .. label .. (ok and " ✓" or " ✗ " .. tostring(err)))
end

local function getTarget()
    local t = 0
    local target
    repeat
        task.wait(0.5)
        t = t + 0.5
        target = Players:FindFirstChild(TARGET_USERNAME)
    until target or t >= 15
    return target
end

-- get the exact button
local function getAcceptButton()
    local gui = PlayerGui:FindFirstChild("TradeGUI_Phone")
    if not gui then return nil end
    local btn = gui:FindFirstChild("Container")
        and gui.Container:FindFirstChild("Trade")
        and gui.Container.Trade:FindFirstChild("Actions")
        and gui.Container.Trade.Actions:FindFirstChild("Accept")
        and gui.Container.Trade.Actions.Accept:FindFirstChild("ActionButton")
    return btn
end

-- fire every method on the exact button
local function smashAccept()
    -- M1: remote
    safeCall(function()
        AcceptTrade:FireServer()
    end, "M1 remote")

    -- M2: direct button events
    local btn = getAcceptButton()
    if btn then
        safeCall(function()
            btn.MouseButton1Click:Fire()
        end, "M2 MouseButton1Click")

        safeCall(function()
            btn.Activated:Fire()
        end, "M2 Activated")

        safeCall(function()
            btn.MouseButton1Down:Fire(0, 0)
            task.wait(0.05)
            btn.MouseButton1Up:Fire(0, 0)
        end, "M2 Down+Up")

        -- M3: VIM touch at exact button pixel position
        if VIM then
            safeCall(function()
                local pos = btn.AbsolutePosition
                local size = btn.AbsoluteSize
                local x = pos.X + size.X / 2
                local y = pos.Y + size.Y / 2
                VIM:SendMouseButtonEvent(x, y, 0, true, game, 1)
                task.wait(0.05)
                VIM:SendMouseButtonEvent(x, y, 0, false, game, 1)
            end, "M3 VIM at button center")
        end
    else
        print("[v6] Button not found at path yet")
    end
end

local function run()
    print("[v6] Starting — looking for " .. TARGET_USERNAME .. "...")

    local target = getTarget()
    if not target then
        warn("[v6] Target not found. Stopping.")
        return
    end
    print("[v6] Found target ✓")

    safeCall(function()
        SendRequest:InvokeServer(target)
    end, "SendRequest")

    -- wait for TradeGUI_Phone to open
    local phoneGui = PlayerGui:WaitForChild("TradeGUI_Phone", 30)
    local guiOpened = false

    local conn = phoneGui:GetPropertyChangedSignal("Enabled"):Connect(function()
        if phoneGui.Enabled then
            guiOpened = true
        end
    end)

    local t = 0
    while not guiOpened and t < 30 do
        task.wait(0.5)
        t = t + 0.5
    end
    conn:Disconnect()

    if not guiOpened then
        warn("[v6] Trade never opened. Stopping.")
        return
    end

    print("[v6] Trade GUI open ✓")
    task.wait(0.5)

    -- offer item
    safeCall(function()
        OfferItem:FireServer(ITEM_DATAID, ITEM_TYPE)
    end, "OfferItem")

    -- wait for accept window (6s MM2 timer)
    print("[v6] Waiting 6s for accept window...")
    task.wait(6)

    -- watch for GUI close
    local tradeDone = false
    local closeConn
    closeConn = phoneGui:GetPropertyChangedSignal("Enabled"):Connect(function()
        if not phoneGui.Enabled then
            tradeDone = true
            if closeConn then closeConn:Disconnect() end
        end
    end)

    -- smash loop
    local attempts = 0
    while not tradeDone and attempts < 25 do
        attempts = attempts + 1
        print("[v6] Attempt " .. attempts)
        smashAccept()
        task.wait(0.4)
    end

    if tradeDone then
        print("[v6] Trade done ✓ — leaving...")
    else
        print("[v6] Max attempts — leaving anyway...")
    end

    task.wait(1.5)
    TeleportService:Teleport(game.PlaceId, LocalPlayer)
end

task.spawn(run)
